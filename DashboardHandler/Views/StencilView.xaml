<UserControl x:Class="DashboardHandler.Views.StencilView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:stencil="clr-namespace:Syncfusion.UI.Xaml.Diagram.Stencil;assembly=Syncfusion.SfDiagram.WPF"
             xmlns:selectors="clr-namespace:DashboardHandler.Selectors"
             xmlns:local="clr-namespace:DashboardHandler.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" Width="350">

    <UserControl.Resources>

        <!--<Style TargetType="stencil:Symbol">
            <Setter Property="Width" Value="100" />
            <Setter Property="Height" Value="45" />
            <Setter Property="Padding" Value="5"/>
            <Setter Property="ToolTip" Value="{Binding Symbol}"/>
        </Style>-->

        <selectors:SymbolTemplateSelector x:Key="SymbolTemplateSelector" />

        <Style TargetType="stencil:Symbol">
            <Setter Property="Width" Value="Auto" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="stencil:Symbol">

                        <Grid MinWidth="200">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <ContentControl Grid.Column="0"
                                            Content="{Binding}"
                                            ContentTemplateSelector="{StaticResource SymbolTemplateSelector}"
                                            MinWidth="70"/>

                            <TextBlock Grid.Column="2" Text="{Binding Name}" VerticalAlignment="Center"/>
                        </Grid>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>
    
    
    <Grid>
        <ContentControl Content="{Binding Stencil}" />
    </Grid>
</UserControl>
